# Agent Instructions — Personal Portfolio Website

This repository contains Bryan's personal portfolio website. The current implementation is a React/TypeScript/Vite application with Tailwind CSS and Framer Motion. The repository is maintained as the portfolio evolves.

## Before Working

1. Read `agent-files/Agents_Context.md` for current working state.
2. Read `agent-files/ARCHITECTURE.md` for finalized decisions.
3. Inspect the existing implementation before making changes.
4. Treat future improvements described in the README as plans, not implemented features, unless the source confirms them.

## Documentation Authority

- `ARCHITECTURE.md` is authoritative for finalized architecture.
- `Agents_Context.md` is temporary working context.
- Do not silently promote proposals or discoveries into architecture.

## Implementation Rules

Preserve existing portfolio content and behavior unless the requested task requires changes. Keep documentation/agent-structure work separate from unrelated source changes.
